# Starter kit bundles
Drop exported `starter-*.json` files here (from Settings → "Export as starter bundle").
On a fresh visit with an empty library, the app hydrates these so the books are
pre-loaded. See PLANxvi Part One.
